# Core dependencies only - minimal setup for scraper to work

# Web Scraping
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# Basic utilities
python-dotenv>=1.0.0
colorama>=0.4.6
tqdm>=4.66.0

# For data processing (we'll add pandas later if needed)
# We can use basic JSON/CSV with stdlib for now
